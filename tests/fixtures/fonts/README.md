`Tuffy.ttf` is the public-domain test font distributed with
[fontdb 0.24.0](https://docs.rs/crate/fontdb/0.24.0/source/tests/fonts/).
See `LICENSE.txt`. It is used only by tests, so text rendering checks do not
depend on the fonts installed on the test machine.
